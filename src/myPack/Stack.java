package myPack;
/*
 * Stack implementation
 * 
 */
public interface Stack //Stack Interface with push and pop methods
{
	void push(int i);// Push used to add item to the top of stack 
	int pop();// Pop deletes item from the top of stack
}
